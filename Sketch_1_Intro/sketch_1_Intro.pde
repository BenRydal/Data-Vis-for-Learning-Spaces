// "Uncomment" lines of code to see how Processing works

void setup() {
  // fullScreen(); // sets size of sketch, also try: size(500, 500);
}

void draw() {
  // ellipse(width/2, height/2, 30, 30);
}

/*
void draw() {
  // background(255);
  int r = 30;
  // fill(50, 100);
  ellipse(mouseX, mouseY, r, r);
  // if (mousePressed) {
  //  line(mouseX, mouseY, pmouseX, pmouseY);
  // }
}
*/
